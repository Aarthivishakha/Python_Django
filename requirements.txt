Django==3.0.14
# Intentionally pinned to an old release with published CVEs (e.g.
# CVE-2015-2296, CVE-2018-18074) - a real dependency of
# catalog/integrations.py, not a decorative pin, and what gives a
# dependency-scanning tool something genuine to find against this
# project's actual dependencies.
requests==2.6.0
