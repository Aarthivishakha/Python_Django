Django==1.5.12
# Intentionally pinned to an old release with published CVEs (e.g.
# CVE-2015-2296, CVE-2018-18074) - a real dependency of
# catalog/integrations.py, not a decorative pin, and what gives a
# dependency-scanning tool something genuine to find against this
# project's actual dependencies.
requests==2.6.0

# --- quality/ tool dependencies (moved here so quality/ contains only
# the 14 tool folders, no other files) ---
beniget==0.5.0
gast>=0.5.0
complexipy>=1.0.0
cosmic-ray==8.4.6
coverage==7.14.1
crosshair-tool>=0.0.109
pip-audit==2.10.1
pydriller>=2.9
pylint==4.0.6
pymcdc==0.2.5
radon==6.0.1
lizard==1.17.10
semgrep>=1.172.0
bandit==1.8.3
pytest-testmon==2.2.0
pytest>=7.0
pytest-django>=3.0
