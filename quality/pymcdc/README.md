# pymcdc — Python 2.6

Checks MC/DC condition coverage of `calculate_order_total`'s compound
boolean decision against the real `catalog/tests.py` suite.

Run from repo root:

```bash
python -m pymcdc --unittest catalog.tests catalog/pricing.py
```
