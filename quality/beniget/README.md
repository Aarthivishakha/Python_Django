# beniget — Python 2.6

Runs beniget's def-use chain analysis over the real `catalog/pricing.py`
module (not a standalone fixture). Requires a Python 3.6+ interpreter to
run beniget itself.

Run from repo root:

```bash
python quality/beniget/run_beniget.py
```
