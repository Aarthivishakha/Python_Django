# cognitive-ast (complexipy) — Python 2.6

Runs complexipy's cognitive-complexity scan over the real
`catalog/pricing.py` module. Requires a Python 3.8+ interpreter to run
complexipy itself.

Run from repo root:

```bash
complexipy catalog/pricing.py
```
