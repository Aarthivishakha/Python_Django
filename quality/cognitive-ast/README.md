# cognitive-ast (complexipy) — Python 3.10

Runs complexipy's cognitive-complexity scan over the real
`catalog/pricing.py` module (including its `match` statement).

Run from repo root:

```bash
complexipy catalog/pricing.py
```
