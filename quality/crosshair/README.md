# crosshair — Python 2.6

Symbolically checks `catalog/pricing.py::calculate_order_total` against
its real input-validation invariants. Requires Python 3.8+ to run
crosshair-tool itself.

Run from repo root:

```bash
crosshair check catalog/pricing.py
```
