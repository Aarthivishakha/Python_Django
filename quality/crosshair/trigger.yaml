tool: crosshair
version: "0.0.109"
language: python
language_version: "3.8"
technique: "Static Analysis Metric"
classification: "Execution Path Integrity"
target_files:
  - ../../catalog/pricing.py
run:
  command: "crosshair check catalog/pricing.py"
requirements:
  - crosshair-tool>=0.0.109
expects:
  contracts_verified: true
notes: >
  calculate_order_total raises ValueError on invalid input and its
  docstring documents the real positive-quantity/non-negative-price
  invariants that crosshair checks against the actual implementation.
