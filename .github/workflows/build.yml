name: Build

on:
  push:
    branches: [PYTHON_3.13]
  pull_request:
    branches: [PYTHON_3.13]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Build
        run: bash build.sh
